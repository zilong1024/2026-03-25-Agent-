# 这个笔试题在做什么（面向非 Agent 背景）

## 1. 先用一句话说明

这道题是在考你能不能把一个“会自动规划并执行量化研究流程的小型 Agent 系统”从**能跑 demo**提升到**更真实、更稳定、可解释**。

它不是单纯做前端/后端，也不是单纯调模型，而是三件事结合：

- 运行时（Runtime）真实接数
- Agent 规划流程更稳
- 工具与元数据设计更适合大模型调用

---

## 2. 项目背景：这个系统是怎么工作的

这个仓库里有两层核心能力：

1. 执行层（YAML Runtime）
- 输入是一条“研究流程”（pipeline）
- 每个步骤叫 step（例如：拉行情、算动量、排序、生成解释）
- 调度器按依赖顺序执行 step，并把上一步输出给下一步用

2. 规划层（Handwritten ReAct Agent）
- 输入是自然语言需求（比如“先算动量再排序再解释”）
- 模型不会直接输出最终答案，而是通过工具调用逐步搭 pipeline
- 最后把 pipeline 交给执行层去跑

---

## 3. Agent 相关概念（简化定义）

## 3.1 什么是 Agent（在本题语境里）

这里的 Agent 不是“万能智能体”，而是：

- 读取用户意图
- 调用一组工具（add/update/connect/get）
- 逐步构建结构化流程（pipeline）
- 在失败时尝试修复，再继续

本质上是“**模型 + 工具 + 控制循环**”。

## 3.2 什么是 ReAct Loop

ReAct 可以理解成“思考 + 行动”的循环：

1. 模型看当前上下文，决定调哪个工具
2. 工具返回结果（成功/失败）
3. 模型根据结果继续修正
4. 满足结束条件后停止

本题要求你保留“手写循环”，不要直接换成现成 Agent 框架。

## 3.3 为什么要有 Tool（工具）层

因为模型直接输出任意文本不可控。  
把动作约束成工具调用后，系统就能：

- 校验参数
- 明确错误
- 保证输出结构可执行

---

## 4. 题目主要需求（官方要求拆解）

## Workstream 1：把两个 mock 节点变成真实集成

1. `data.market_bars`
- 用 BaoStock 拉日线
- 支持 `symbols` 和 `lookback_days`
- 返回按 symbol 分组的 bar 数据
- 查询失败要清晰报错

2. `research_chat`
- 调真实 chat completions 接口
- 支持 `prompt`（可选 model）
- 返回结构化文本结果
- API 失败要清晰报错

## Workstream 2：提升 Agent Loop 稳定性

需要解决的问题：

- 无效回复后容易停住
- 错误恢复差
- 终止条件不清晰，可能空转或过早结束

考察点：

- 消息序列组织
- tool 结果处理
- 失败恢复
- 终止策略

## Workstream 3：在“通用工具”前提下提升规划成功率

不能靠加一堆专用工具“作弊”，要在现有通用工具上提升效果：

- `add_step / update_step / connect_steps / get_catalog / get_details / get_pipeline`

核心是改进：

- tool 描述
- schema 清晰度
- catalog 元数据质量
- 对开放配置字典的引导能力

---

## 5. 它到底在考候选人的什么能力

1. 系统化工程能力
- 不是只会写功能，还要处理错误、终止、可维护性

2. LLM 工程能力
- 会不会设计工具契约
- 会不会让模型在受限动作下稳定完成任务

3. 数据与运行时能力
- 真正接入外部数据/API，不停留在 mock

4. 代码阅读与增量改造能力
- 在陌生仓库里快速定位关键点并改出可运行结果

---

## 6. 你可以用什么结果判断“做对了”

最低标准：

- 公共测试通过（`tests/run_tests.py`）
- 两个 mock 节点已真实化
- loop 出现错误时不会立刻崩掉或无限循环

更强信号：

- 给定目标提示能较稳定产出“市场数据 -> 动量 -> 排序 -> 解释”的完整链路
- 日志和错误信息足够清晰，可用于面试复盘

---

## 7. 这份题目的实战意义

真实业务里，Agent 项目最常见失败点恰好就是这题覆盖的三件事：

- 真实数据接入不稳
- 规划过程不可控
- 工具设计不利于模型理解

所以这题本质是在验证：  
你是否能把“看起来能演示”的 Agent，推进到“可以持续迭代的工程基线”。
